#include <stdio.h>

void display(char name[20])
  
int main(){
  char user_name[20];
  scanf("%s", name);
  display(name);
  return 0;
           }

void display(char name[20]){
printf("Hello %s", name);
}
